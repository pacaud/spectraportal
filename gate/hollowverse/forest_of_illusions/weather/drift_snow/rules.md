# drift_snow — rules

status: seeded
type: rules
parent: drift_snow
region: forest_of_illusions

[rules]
- Drift Snow is never dangerous-icy by default.
- Visibility stays usable (softened, not blinding).
- If a scene needs threat, use a different weather entry.
